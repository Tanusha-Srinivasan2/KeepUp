name: keep_up_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # ✅ Google & Firebase
  google_sign_in: ^6.1.5
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0

  # ✅ UI & Fonts
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.3
  flutter_card_swiper: ^7.2.0

  # ✅ Logic & Networking
  http: ^1.6.0
  shared_preferences: ^2.5.4
  uuid: ^4.5.2
  intl: ^0.19.0 # Required for Date Formatting

  # ✅ Hardware & Utilities
  url_launcher: ^6.3.2 # Only list this ONCE (and use the newer version)
  flutter_tts: ^4.2.3
  speech_to_text: ^7.3.0
  permission_handler: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
